/// AtlasQL abstract syntax tree type definitions.
///
/// Defines the node types produced by the parser: SELECT, FROM (with source
/// annotations), WHERE, JOIN, GROUP BY, ORDER BY, LIMIT, PIPE operators
/// (AI_SENTIMENT, AI_SUMMARIZE, etc.), and LET bindings.
pub struct Ast;
